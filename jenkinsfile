pipeline {
    agent {label 'docker'}
    stages {
        stage  {
            steps {
                git branch :"master",url:"https://github.com/ramesh1469/springpetclinic.git"
            }

        }
        stage  {
            steps {
                sh 'docker info'
            }

        }
    }
}